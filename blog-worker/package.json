{
  "name": "blog-worker",
  "version": "1.0.0",
  "private": true,
  "description": "blog-worker: proxy GitHub Pages + admin publish + waline comments",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}